{% extends "base.html.twig" %}
{%  block body %}
    <div class="container my-5">
        <h1>Inscription</h1>

        {{ form_start(form) }}
        <div class="d-flex p-2 bd-highlight">
            {{ form_row(form.firstname) }}
            {{ form_row(form.lastname) }}
        </div>
        {{ form_end(form) }}
        <div>
            <a href="{{ path("app_login") }}">Vous avez un compte ? Vous connectez</a>
        </div>
    </div>
{% endblock %}